<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>URL Shortener</title>
    <style>
        *{
            box-sizing: border-box;
            font-family: Verdana, Geneva, Tahoma, sans-serif;
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <h1 class="text-center mt-4 display-1">URL Shortener</h1>
        <form method="POST" action="/url">
            <div class="mb-3">
              <label for="shortenURL" class="form-label">Enter your URL</label>
              <input type="input" name="url" class="form-control" id="shortenURL" aria-describedby="emailHelp">
              <div id="shortenURL" class="form-text">Shorten your URL</div>
            </div>
            <button type="submit" class="btn btn-primary">Generate</button>
          </form>
          <div class="text-center h-3">
            <% if (locals.msg) { %> <!--locals is getting data from database-->
              Generated URL: <%= msg %>
            <% } %>
          </div>
          <div>
            <% if (locals.urls) { %>
                Generated URLS:
                <table class="table">
                    <thead>
                        <th>Sr.No</th>
                        <th>Short URL</th>
                        <th>Redirect URL</th>
                        <th>Click</th>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, index) => { %>
                            <tr>
                            <td><%= index + 1 %></td>
                            <td><%= url.shortID %></td>
                            <td><%= url.redirectURL %></td>
                            <td><%= url.visitedHistory.length %></td>
                        </tr>
                        <% }) %>
                    </tbody>
                </table>
            <% } %>
          </div>
    </div>
</body>
</html>